# Spring Boot 앱 연결
upstream backend {
    server app:8080;
}

# HTTP 서버 (SSL 설치 전)
server {
    listen 80;
    server_name _;  # TODO: 실제 도메인으로 변경 (예: api.yourdomain.com)

    # Let's Encrypt 인증서 검증용
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API 요청을 Spring Boot로 전달
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS 설정 (CloudFront용)
        # TODO: CloudFront URL로 변경
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;
        
        # OPTIONS 요청 처리 (Preflight)
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }
}

# HTTPS 서버 (SSL 인증서 설치 후 주석 해제)
# server {
#     listen 443 ssl http2;
#     server_name api.yourdomain.com;
# 
#     # SSL 인증서
#     ssl_certificate /etc/letsencrypt/live/api.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.yourdomain.com/privkey.pem;
# 
#     # API 요청
#     location / {
#         proxy_pass http://backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # CORS 설정
#         add_header 'Access-Control-Allow-Origin' 'https://yourdomain.cloudfront.net' always;
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
#         add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;
#         
#         if ($request_method = 'OPTIONS') {
#             return 204;
#         }
#     }
# }
