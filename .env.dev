# ========================================
# 프로덕션 환경 변수 예시 (.env.prod.example)
# 실제 .env.prod 파일은 절대 Git에 커밋하지 말 것!
# 실제 배포 서버에서 .env.prod 파일을 생성하고 안전하게 관리
# ========================================

# Database 설정 (실제 값으로 변경 필요)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=musinsa_dev
DB_USERNAME=musinsa_user
DB_PASSWORD=Qwer1234!

# JWT 설정 (강력한 시크릿 키 필요 - 256bit 이상)
# 아래 명령어로 안전한 키 생성: openssl rand -base64 64
JWT_SECRET=REPLACE_THIS_WITH_SECURE_PRODUCTION_JWT_SECRET_KEY_MINIMUM_256_BITS_REQUIRED
JWT_ACCESS_EXPIRATION=1800000
JWT_REFRESH_EXPIRATION=1209600000

# Server 설정
SERVER_PORT=8080

# AWS 설정 (필요시)
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=ap-northeast-2
# S3_BUCKET_NAME=musinsa-prod-bucket

# Redis 설정 (필요시)
# REDIS_HOST=redis.musinsa.com
# REDIS_PORT=6379
# REDIS_PASSWORD=redis-password

# 외부 API 키 (필요시)
# PG_API_KEY=payment-gateway-api-key
# SMS_API_KEY=sms-service-api-key
# EMAIL_API_KEY=email-service-api-key

# 모니터링 설정 (필요시)
# SENTRY_DSN=https://...
# ELASTIC_APM_SERVER_URL=https://...
