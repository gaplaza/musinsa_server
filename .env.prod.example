# 프로덕션 환경 변수 예시 (.env.prod.example)
# 실제 .env.prod 파일은 절대 Git에 커밋하지 말 것!
# 실제 배포 서버에서 .env.prod 파일을 생성하고 안전하게 관리

# Database 설정 (실제 값으로 변경 필요)
DB_HOST=your-production-db-host.aws.com
DB_PORT=3306
DB_NAME=musinsa_prod
DB_USERNAME=musinsa_prod_user
DB_PASSWORD=your-strong-production-password-here

# JWT 설정 (강력한 시크릿 키 필요 - 256bit 이상)
JWT_SECRET=your-super-secure-production-jwt-secret-key-minimum-256-bits-required
JWT_ACCESS_EXPIRATION=1800000
JWT_REFRESH_EXPIRATION=1209600000

# 기타 설정
SERVER_PORT=8080

# AWS/클라우드 설정 (필요시)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=ap-northeast-2
